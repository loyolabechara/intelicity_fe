import 'package:flutter/material.dart';

class SolicitacaoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Solicitação de Serviços'),
      ),
      body: Text('Cidadão pode fazer solicitações diversas'),
    );
  }
}