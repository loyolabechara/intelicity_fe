import 'package:flutter/material.dart';

class EmpregoPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Balcão de Empregos'),
      ),
      body: Text('Relação de oferta de empregos.'),
    );
  }
}